#!/usr/bin/python
# -*- coding:utf-8 -*-
import cherrypy
import os

class HelloWorld:
    # 定义一个index方法
    def index(self):
        return "Hello, world!"
    # 将index方法发布到web
    index.exposed = True

# 把根对象交给cherrypy（不过这里总共就一个对象而已）
cherrypy.quickstart(HelloWorld())
